# starterkit 테스트용 임시 폴더 — 실제 프로젝트는 starterkit 밖으로 옮긴다
_test/

# macOS
.DS_Store

# 에디터·OS
.vscode/
.idea/
*.swp
*~

# Python (scripts/sync_confluence.py 용 venv)
__pycache__/
*.py[cod]
.venv/
.venv-confluence/
venv/

# Confluence sync dry-run 산출물
_dryrun_confluence/

# Environment / secrets — .env.confluence.local 등 (.example 만 커밋)
.env
.env.local
.env.*.local
*.pem
*.key

# 로그
*.log

# 프로젝트별 작업 일지 — 로컬에만 두고 공유하지 않는다 (킷 메커니즘이 아니라 기록물)
DEVLOG.md
HISTORY.md
SHARE.md

# UX 검토 개인 로그 — 공유하지 않는 개인 기록물 (ux-review 스킬 산출)
docs/UX-REVIEW.md

# 토큰 사용량 리포트 — scripts/ 가 매일 자동 생성하는 기록물 (로컬 전용)
token-usage.md
token-usage-combined.md

# PACK-SLOT:bottom — 팩 설치 시 여기 아래에 팩별 gitignore 항목이 추가됨
